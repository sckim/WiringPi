{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/build/Debug/outDebug",
                "-lwiringPi",
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}